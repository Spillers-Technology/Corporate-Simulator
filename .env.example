# Copy to .env and adjust for local use. See docker-compose.yml.

# Local path to your own real board-process data directory. Leave unset (or
# pointed at ./demo-data) to run against the bundled, redacted demo fixture only.
DATA_DIR=./demo-data

BACKEND_PORT=4000
FRONTEND_PORT=3000
